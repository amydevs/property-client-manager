import Vue from 'vue'
import Vuex from 'vuex'

import Clients from '@/store/modules/ClientsDB'

Vue.use(Vuex)

const store = new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Clients
  }
})
export default store